# Memory — 2026-03-29

## OpenCommand Center Agent System

Built a multi-agent framework with dynamic spawning and UI positioning:

- **AgentManager.js** – central registry; loads `agent_registry.json`, instantiates agent classes, handles dispatch
- **agent_registry.json** – full agent manifest: id, name, role, class, position (x,y,panel), icon, permissions, capabilities (10 agents)
- **AgentBase.js** – abstract base class with lifecycle (`initialize`, `process`), state, logging, permissions, event emit, `getStatus()`
- **ExecutiveAgent.js** – CEO-level agent; approves, escalates, sets strategy; emits events

**All 10 agents implemented:**
- Executive (CEO) – left panel
- Ops Commander – left panel
- Finance Agent (Treasury) – left panel
- Bitcoin Agent (Unisat ordinals) – center panel
- Fractal Agent – center panel
- Litecoin Agent (NodesNow Litescribe) – center panel
- Dogecoin Agent (NodesNow Doginals) – center panel
- Tezos Agent (objkt.com NFT) – right panel
- Music Producer – right panel
- Security Officer – right panel

Framework fully built; agents spawn from registry and expose `handle(payload)` for task dispatch.

---

## Blockchain Integration Update

- **Unisat API** integrated for Bitcoin and Fractal Bitcoin ordinals via `UnisatService.js`
- **NodesNow API** integrated for Litecoin, Dogecoin, Tezos via `nodesnow_client.js`
- Agents created: `agents/BitcoinOrdinalAgent.js`, `agents/LitecoinAgent.js`, `agents/DogecoinAgent.js`, `agents/FractalBitcoinAgent.js`, `agents/TezosAgent.js`
- `.env` updated with `UNISAT_API_KEY` and `FRACTAL_API_KEY`
- Testnet funding discussion: user provided UniSat Taproot address but needs testnet mode verification; remaining addresses pending; live test run deferred

---

## Agent Implementations

- **FinanceAgent.js** – stub actions: `process_payment`, `mint_nft`, `balance_check`
- **OperationsAgent.js** – stub actions: `orchestrate`, `monitor`, `schedule`, `deploy`
- **SecurityAgent.js** – stub actions: `scan_transactions`, `risk_assessment`, `alert`, `validate`
- **TezosAgent.js** – stub actions: `send_payment`, `mint_objkt_nft`, `get_balance`
- **FractalBitcoinAgent.js** – stub actions: `inscribe_ordinal`, `get_balance` (uses UnisatService with fractal network)

All agents extend `AgentBase` and require `initialize()` and `handle(payload)`; status available via `getStatus()`.

---

## Key Decisions

- Use custodial API signing (Unisat/NodesNow) to avoid private key management
- Agent UI layout: three columns (left, center, right) with explicit (x,y) coordinates
- Executive agent holds admin/override permissions
- Each agent declares capabilities; permissions drive access control
- Dispatching via `AgentManager.handle(agentId, payload)` routes to correct agent

---

## Next Steps

- Replace stub implementations with real API calls (Unisat/NodesNow)
- Wire WebSocket/event bus for cross-agent communication
- Add task queuing and retry (resilient mode)
- Build multi-OpenClaw interconnect (WebSocket federation)

---

## Web UI Implementation

- `index.html`: three-panel layout, modal for task input, status bar
- `style.css`: dark theme, agent cards, responsive grid
- `frontend.js`: loads `agent_registry.json`, renders cards, POSTs to `/agents/:id/task`
- `server.js`: optional Express server (static files + task dispatch proxy)
- UI shows agent icon, name, role, status (idle/active/error), and a "Send Task" button

To run:
```bash
npm install express
node server.js
```
Open http://localhost:3000

---

## GitHub Deployment

- Created repository `sidekickadventures/opencommand-center`
- Initial commit: core framework + registry + blockchain services + 5 agents
- Second commit: web UI (index.html, style.css, frontend.js, server.js) + README
- Third commit: implemented remaining 5 agent classes (Finance, Operations, Security, Tezos, FractalBitcoin)
- Branch: `master`
- Remote: `https://github.com/sidekickadventures/opencommand-center.git`

---

## Feature Request

- **Multi-OpenClaw Interconnect:** Allow users to connect multiple OpenClaw instances and external AI agents to the OpenCommand Center so they can interact and collaborate.
  - Distributed agent network: agents from different hosts register and communicate.
  - Approach: WebSocket server with authentication; agents connect as nodes; Executive orchestrates.
  - Needs: message routing, security (shared secret/OAuth), discovery.
  - Roadmap: after stubs are replaced with real integrations.
